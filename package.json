{
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.2",
    "rollup": "^2.75.7",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "build": "npm run test && npm run compile && npm run rollup",
    "test": "jest",
    "compile": "tsc",
    "rollup": "rollup dist/main.js --format umd --name 'sid-js' --sourcemap --file dist/index.js"
  }
}
